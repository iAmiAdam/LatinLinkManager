<% provide(:title, 'All Translators') %>
<div class="col-xs-12 col-sm-12 col-md-10 col-lg-8 col-md-offset-1 col-lg-offset-2">
	<div class="well">
		<tbody>
		<table class="table table-hover">
			<tr><td>Name</td><td>Email</td><td>Rate</td><td>Delete</td></tr>
			<% @translators.each do |user| %>
			<tr><td><%= translator.name %></td><td><%= translator.email %></td><td><%= translator.rate %></td><td><%= link_to content_tag('i', '', class: 'fa fa-trash-o fa-2x'), translator.id, data: { confirm: "Are you sure you want to delete this manager?" }, method: :delete %></span></td></tr>
			<% end %>
		</table>
		</tbody>
	</div>
</div>